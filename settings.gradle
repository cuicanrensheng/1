pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 删除 flatDir berry_lib 配置，改用子模块依赖
    }
}
rootProject.name = "1"
include ':app'
include ':berry_lib'
