pluginManagement {
    repositories {
        // 移除不稳定maven.google镜像，只用公共库
        maven { url 'maven { url "https://mirrors.aliyun.com/android.google/" }' }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven { url 'maven { url "https://mirrors.aliyun.com/android.google/" }' }
        google()
        mavenCentral()
    }
}
rootProject.name = "AH"
include ':app'
