buildscript {
    repositories {
        google()  // 关键：添加Google Maven仓库
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.4.0"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.20"
    }
}

allprojects {
    repositories {
        google()  // 关键：添加Google Maven仓库
        mavenCentral()
    }
}
